apiVersion: v1
kind: Service
metadata:
  name: app
  namespace: lanchonete
  labels:
    app: product
    environment: production
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-type: "alb"
spec:
  type: LoadBalancer
  selector:
    app: product
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080